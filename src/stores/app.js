import { reactive } from 'vue'

/**
 * Store partagé global.
 * Ajouter ici les données réactives partagées entre les pages.
 */
export const appStore = reactive({
  // Exemple :
  // items: [],
  // settings: {}
})
